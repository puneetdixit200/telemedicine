<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title || 'Telemedicine' %></title>
  <link rel="stylesheet" href="/public/css/tailwind.css" />
  <link rel="stylesheet" href="/public/css/styles.css" />
</head>
<body data-authenticated="<%= user ? 'true' : 'false' %>" data-role="<%= user ? user.role : 'guest' %>">
  <div class="app-shell">
  <div class="container">
    <div class="nav app-nav">
      <% if (user) { %>
        <a class="nav-link" href="/dashboard">Dashboard</a>
        <% if (user.role !== 'doctor') { %>
          <a class="nav-link" href="/doctors">Doctors</a>
        <% } %>
        <a class="nav-link" href="/appointments">Appointments</a>
      <% } %>
      <div class="spacer"></div>
      <select id="languageSelector" aria-label="Language selector">
        <option value="en">English</option>
        <option value="hi">Hindi</option>
        <option value="bn">Bengali</option>
        <option value="gu">Gujarati</option>
        <option value="kn">Kannada</option>
        <option value="ml">Malayalam</option>
        <option value="mr">Marathi</option>
        <option value="pa">Punjabi</option>
        <option value="ta">Tamil</option>
        <option value="te">Telugu</option>
        <option value="ur">Urdu</option>
      </select>
      <div id="google_translate_element" class="translate-hidden" aria-hidden="true"></div>
      <% if (user) { %>
        <span id="selfPresenceBadge" class="badge presence-badge <%= user.isCallOnline ? 'presence-online' : 'presence-offline' %>"><%= user.isCallOnline ? 'online' : 'offline' %></span>
        <span class="badge role-badge role-<%= user.role %>"><%= user.role %></span>
        <span class="nav-user-name"><%= user.fullName %></span>
        <form method="post" action="/auth/logout" style="margin:0;">
          <button class="secondary logout-btn" type="submit">Logout</button>
        </form>
      <% } else { %>
        <a class="nav-link" href="/auth/login">Login</a>
        <a class="nav-link" href="/auth/register">Register</a>
      <% } %>
    </div>

    <%- body %>
  </div>

  <script src="/public/js/presence-ping.js"></script>
  <script src="/public/js/translation.js"></script>
  </div>
</body>
</html>
