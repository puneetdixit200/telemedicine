<%- include('partials/layout', { title: 'Register', user, body: `
<div class='card'>
  <p class='page-subtitle' style='text-transform:uppercase; letter-spacing:0.08em; margin:0 0 6px;'>Create Account</p>
  <h2 class='page-title'>Register</h2>
  ${error ? `<p class='error'>${error}</p>` : ''}
  <form method='post' action='/auth/register' style='margin-top:12px;'>
    <div class='row'>
      <div>
        <label>Full name</label>
        <input name='fullName' required />
      </div>
      <div>
        <label>Email</label>
        <input name='email' type='email' required />
      </div>
      <div>
        <label>Phone</label>
        <input name='phone' />
      </div>
      <div>
        <label>Password</label>
        <input name='password' type='password' required />
      </div>
      <div>
        <label>Role</label>
        <select name='role' required>
          <option value='patient'>Patient</option>
          <option value='doctor'>Doctor</option>
          <option value='admin'>Admin</option>
        </select>
      </div>
      <div>
        <label>Admin invite code (only if admin)</label>
        <input name='adminInviteCode' />
      </div>
      <div>
        <label>Gender</label>
        <input name='gender' />
      </div>
      <div>
        <label>Date of birth</label>
        <input name='dateOfBirth' type='date' />
      </div>
      <div>
        <label>Address</label>
        <input name='address' />
      </div>
      <div>
        <label>Language preference</label>
        <input name='language' />
      </div>
      <div>
        <label>Time zone</label>
        <input name='timeZone' placeholder='e.g. Asia/Kolkata' />
      </div>
    </div>

    <div class='card'>
      <h3 style='margin:0 0 8px;'>Doctor details (only if role=doctor)</h3>
      <div class='row'>
        <div><label>Specialization</label><input name='specialization' /></div>
        <div><label>Years of experience</label><input name='yearsOfExperience' type='number' min='0' /></div>
        <div><label>Qualifications</label><input name='qualifications' /></div>
        <div><label>Clinic/Hospital name</label><input name='clinicName' /></div>
        <div><label>Consultation languages</label><input name='consultationLanguages' placeholder='English,Hindi' /></div>
      </div>
      <div style='margin-top:10px;'><label>Description</label><textarea name='description' rows='2'></textarea></div>
    </div>

    <div style='margin-top:12px;'>
      <button type='submit'>Create account</button>
    </div>
  </form>
  <p class='page-subtitle' style='margin-top:12px;'>Already have an account? <a href='/auth/login'>Login</a></p>
</div>
` }) %>
